Rodar o Backend:

cd backend
..\venv\Scripts\activate
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload



Rodar o Frontend

cd mobile
..\venv-mobile\Scripts\activate
python main.py


"base_url": "http://127.0.0.1:8000"


Logs do Backend:

Os eventos da API sao exibidos no terminal e gravados em:

backend/logs/nfe_scanner.log

Configuracoes opcionais no backend/.env:

LOG_LEVEL=INFO
LOG_DIR=logs

Niveis recomendados:

INFO = operacoes normais, consultas, cadastros e tempo das requisicoes
WARNING = nota nao encontrada, dados invalidos e conflitos
ERROR = falhas de conexao, configuracao ou integracao

As chaves de acesso das notas sao mascaradas nos logs e a chave privada da API fiscal nao e registrada.


Painel Web:

http://127.0.0.1:8000/painel

O painel apresenta a tabela completa de notas com atualizacao automatica, filtros,
leitura por chave, conferencia, edicao, exclusao, geracao de XML e cadastro de faturistas.




API

$apiKey = "nfe_scanner_api_2026_9XqP7mL4vR2sK8dW6zN3bY5t"

Invoke-RestMethod `
  -Uri "https://nfe-scanner-api.onrender.com/api/v1/notas?bip_inicio=2026-08-01&bip_fim=2026-08-31&page=1&page_size=5" `
  -Headers @{ "X-API-Key" = $apiKey }



  Invoke-RestMethod `
  -Uri "https://nfe-scanner-api.onrender.com/api/v1/notas/31260833592510044798550020002889051384488548" `
  -Headers @{ "X-API-Key" = $apiKey }


  Invoke-RestMethod `
  -Uri "https://nfe-scanner-api.onrender.com/api/v1/notas-excluidas?exclusao_inicio=2026-08-01&exclusao_fim=2026-08-31&page=1&page_size=5" `
  -Headers @{ "X-API-Key" = $apiKey }